# ConfigMap — fixed configuration to resolve CrashLoopBackOff
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: scenario-01-config
  namespace: default
  labels:
    scenario: "01-crashloop-bad-config"
data:
  DATABASE_URL: "postgresql://user:password@db-host:5432/mydb"
  APP_PORT: "8080"
  LOG_LEVEL: "INFO"
