---
apiVersion: v1
kind: Service
metadata:
  name: scenario-07-dns-check
  namespace: test-scenerios
  labels:
    scenario: "07-dns-resolution-failure"
spec:
  selector:
    app: scenario-07
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
